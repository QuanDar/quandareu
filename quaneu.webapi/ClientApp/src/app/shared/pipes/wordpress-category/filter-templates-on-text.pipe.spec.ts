import { FilterTemplatesOnTextPipe } from './filter-templates-on-text.pipe';

describe('FilterTemplatesOnTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTemplatesOnTextPipe();
    expect(pipe).toBeTruthy();
  });
});
