import { WpTemplCatPipe } from './wp-templ-cat.pipe';

describe('WpTemplCatPipe', () => {
  it('create an instance', () => {
    const pipe = new WpTemplCatPipe();
    expect(pipe).toBeTruthy();
  });
});
